/target/
# makepkg leaves these in packaging/arch
/packaging/arch/src/
/packaging/arch/pkg/
/packaging/arch/tango/
/packaging/arch/*.pkg.tar*
/packaging/arch/*.log
log.txt
# raw Claude Code transcripts stay local; only the .md summaries are committed (#151)
/docs/sessions/*.jsonl
